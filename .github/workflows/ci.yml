name: CI

on:
  push:
    branches:
      - main
      - "phase4/**"
      - "phase5/**"
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    name: Smoke
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI smoke check"