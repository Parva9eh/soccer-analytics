# Optional root env for docker compose (copy to .env in repo root).
# For the real Supabase stack, prefer apps/api/.env from apps/api/.env.example.

# Web (passed into the web container)
NEXT_PUBLIC_AUTH_ENABLED=false
# NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here